<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.custom_theme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/gunmetal</item>
        <item name="colorPrimaryVariant">@color/black</item>
        <item name="colorOnPrimary">@color/mint_cream</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/black</item>
        <item name="colorSecondaryVariant">@color/jet</item>
        <item name="colorOnSecondary">@color/silver_chalice</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <!-- Customize your theme here. -->
        <item name="android:textColor">@color/black</item>
        <item name="android:editTextColor">@color/black</item>
        <item name="android:iconTint">@color/mint_cream</item>
        <item name="cardBackgroundColor">@color/mint_cream</item>
        <item name="android:windowBackground">@drawable/mybackground</item>
        <item name="cardCornerRadius">10dp</item>
        <item name="android:dialogCornerRadius">10dp</item>
    </style>
</resources>